commit e37fcee35d3124a98e90c220fa6cb5c0862fd296
Author: Runxi Yu <me@runxiyu.org>
Date:   Thu Jun 18 14:28:39 2026 +0000

    fix(sni): ignore name/pixmap updates for custom icons

diff --git a/include/modules/sni/item.hpp b/include/modules/sni/item.hpp
--- a/include/modules/sni/item.hpp
+++ b/include/modules/sni/item.hpp
@@ -41,6 +41,7 @@ class Item : public sigc::trackable {
   std::string title;
   std::string icon_name;
   Glib::RefPtr<Gdk::Pixbuf> icon_pixmap;
+  bool has_custom_icon_ = false;
   Glib::RefPtr<Gtk::IconTheme> icon_theme;
   std::string overlay_icon_name;
   std::string attention_icon_name;
diff --git a/src/modules/sni/item.cpp b/src/modules/sni/item.cpp
--- a/src/modules/sni/item.cpp
+++ b/src/modules/sni/item.cpp
@@ -178,9 +178,17 @@ void Item::setProperty(const Glib::ustring& name, Glib::VariantBase& value) {
     } else if (name == "Status") {
       setStatus(get_variant<Glib::ustring>(value));
     } else if (name == "IconName") {
-      icon_name = get_variant<std::string>(value);
+      if (has_custom_icon_) {
+        spdlog::trace("Item '{}': ignoring IconName update, custom icon is set", id);
+      } else {
+        icon_name = get_variant<std::string>(value);
+      }
     } else if (name == "IconPixmap") {
-      icon_pixmap = this->extractPixBuf(value.gobj());
+      if (has_custom_icon_) {
+        spdlog::trace("Item '{}': ignoring IconPixmap update, custom icon is set", id);
+      } else {
+        icon_pixmap = this->extractPixBuf(value.gobj());
+      }
     } else if (name == "OverlayIconName") {
       overlay_icon_name = get_variant<std::string>(value);
     } else if (name == "OverlayIconPixmap") {
@@ -237,5 +237,14 @@ void Item::setCustomIcon(const std::string& id) {
       try {
-        Glib::RefPtr<Gdk::Pixbuf> custom_pixbuf = Gdk::Pixbuf::create_from_file(custom_icon);
+        GError* error = nullptr;
+        auto custom_pixbuf = Glib::wrap(gdk_pixbuf_new_from_file_at_scale(
+            custom_icon.c_str(), static_cast<int>(getScaledIconSize()),
+            static_cast<int>(getScaledIconSize()), true, &error));
+        if (error != nullptr) {
+          spdlog::error("Failed to load custom icon {}: {}", custom_icon, error->message);
+          g_error_free(error);
+        } else {
         icon_name = "";  // icon_name has priority over pixmap
         icon_pixmap = custom_pixbuf;
+          has_custom_icon_ = true;
+        }
       } catch (const Glib::Error& e) {
@@ -246,4 +256,5 @@ void Item::setCustomIcon(const std::string& id) {
      } else {  // if file doesn't exist it's most likely an icon_name
        icon_name = custom_icon;
+      has_custom_icon_ = true;
      }
 }
